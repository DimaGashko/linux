bindsym Ctrl+$fn $exec dunstctl close
bindsym Ctrl+Shift+$fn $exec dunstctl close-all
bindsym Ctrl+grave $exec dunstctl history-pop
bindsym Shift+$fn $exec dunstctl context

bindsym $mod+r $exec dunstctl close-all